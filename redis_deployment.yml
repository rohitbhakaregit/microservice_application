apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-pod
spec:
  replicas: 2
  selector:
    matchLabels:
      name: redis-pod
  template:
    metadata:
      labels:
        name: redis-pod
    spec:
      containers:
      - name: redis-pod
        image: redis